import { Button } from "antd";

const PagePanel = () => {
    return (
        <div>
            <h2>Page title</h2>
            <Button>Export</Button>
            <Button>Notes</Button>
            <Button>Filter</Button>
        </div>
    );
}

export default PagePanel;